# Langfuse Helm Values for AKS - MINIMAL
langfuse:
  salt:
    value: "hybrid-ai-platform-salt-2026"
  nextauth:
    url: "http://localhost:3000"
    secret:
      value: "hybrid-ai-platform-langfuse-secret-2026"
  features:
    telemetryEnabled: false

# PostgreSQL - Azure
postgresql:
  deploy: false
  host: "psql-ai-platform-91vaoc.postgres.database.azure.com"
  port: 5432
  args: "sslmode=require"
  auth:
    database: "langfuse"
    username: "pgadmin"
    password: "Iyj*BQ7AL1HPf<7F[SyR7_IE"

# ClickHouse - minimal
clickhouse:
  deploy: true
  auth:
    username: default
    password: "clickhouse-langfuse-2026"
  replicaCount: 1
  shards: 1
  zookeeper:
    replicaCount: 1
